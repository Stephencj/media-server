package com.mediaserver.tv

// Copy this file to `Secrets.kt` (already gitignored) and fill in the
// values. Each platform build (iOS / tvOS / Fire TV) gets its own API key,
// minted on the server via the `device_keys` table. Plaintext lives only
// in this gitignored file; the server stores SHA-256 hashes.
object Secrets {
    const val API_KEY = "PUT-YOUR-FIRETV-DEVICE-KEY-HERE"
    const val SERVER_URL = "https://watch.refinedaf.com"
}
