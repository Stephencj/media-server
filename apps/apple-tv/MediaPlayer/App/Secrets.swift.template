import Foundation

// Copy this file to `Secrets.swift` (already gitignored) and fill in the
// values. Each platform build (iOS / tvOS / Fire TV) gets its own API key,
// minted on the server via the `device_keys` table. Plaintext lives only
// in this gitignored file; the server stores SHA-256 hashes.
enum Secrets {
    static let apiKey = "PUT-YOUR-TVOS-DEVICE-KEY-HERE"
    static let serverURL = "https://watch.refinedaf.com"
}
